import { CreatePost } from "../Components/CreatePost"

export const CreatePostPage = () => {
    return(
        <div>
            <h2> 
                Create New Post
                <CreatePost/>
            </h2>
        </div>
    )
}